# Ufinity-Assesment
RESTful Node App 

Completely written in ES6 syntax, so babel-node is required.

# Run instructions
npm install

create .env based on .env.default

npm start

# Test instructions
npm test
